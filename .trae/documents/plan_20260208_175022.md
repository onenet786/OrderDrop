
I will implement the "Order Wise Sale Summary" report in the existing "Generate New Report" workflow.

### 1. Frontend: Admin HTML Updates (`admin.html`)
*   **Add Report Type**: Add "Order Wise Sale Summary" to the dropdown list in the "Generate Financial Report" modal.
*   **Add Store Filter**: Add a "Select Store" dropdown (hidden by default) to the modal, similar to the existing Rider filter.

### 2. Frontend: JavaScript Logic (`js/financial.js`)
*   **Store Filter Logic**: Implement `populateReportStoresDropdown` to load stores into the new dropdown.
*   **Event Handling**: Update the modal's change listener to show/hide the Store and Rider filters based on the selected report type.
    *   *Order Wise Sale Summary* will show both Rider and Store filters.
*   **Report Generation**: Update `submitGenerateReport` to send the selected Store ID and Rider ID to the backend.
*   **Report Viewer**: Update the `viewReport` function to render the new report data in a table format with the following columns:
    *   Order #
    *   Items (Name × Qty @ Price)
    *   Item Sales (Total Price)
    *   Item Cost (Total Cost Price)
    *   Commission
    *   Delivery Charges
    *   Total Amount (Item Sales + Delivery)
    *   **Summary Row**: Displaying totals for Sales, Cost, Commission, and Delivery Charges.

### 3. Backend: Report Generation API (`routes/financial.js`)
*   **Update Endpoint**: Modify `POST /api/financial/reports/generate` to handle the `order_wise_sale_summary` type.
*   **SQL Query**:
    *   Join `orders`, `order_items`, `products`, `stores`, and `riders`.
    *   Filter by Date Range, Store (optional), and Rider (optional).
    *   Select `order_number`, `created_at`, item details (name, quantity, price, cost), `delivery_fee`, and calculated commission.
    *   Aggregate item data to calculate "Total Cost" (Product Cost) and "Item Sales".

### Verification
*   Reload the page.
*   Open "Generate New Report".
*   Select "Order Wise Sale Summary".
*   Verify Store and Rider filters appear.
*   Generate the report and verify the data in the preview modal.
*   Save the report and verify it appears in the list.
